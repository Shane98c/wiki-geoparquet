[project]
name = "wiki-geoparquet"
version = "0.1.0"
description = "All geotagged English Wikipedia articles as GeoParquet + PMTiles"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.0.0",
    "geoparquet-io",
    "pyarrow>=15.0.0",
]
